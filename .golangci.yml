version: "2"

run:
  timeout: 5m
  go: '1.25'

formatters:
  enable:
    - gofmt
    - goimports

linters:
  exclusions:
    rules:
      # Exclude generated Swagger docs from linting
      - path: ^docs/
        linters:
          - errcheck
          - unused
          - govet
