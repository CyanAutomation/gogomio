version: "2"

run:
  timeout: 5m
  go: '1.23'

formatters:
  enable:
    - gofmt
    - goimports

issues:
  exclude-rules:
    # Exclude generated Swagger docs from linting
    - path: ^docs/
      linters:
        - errcheck
        - unused
        - govet
